Meteor.startup(function(){
	// Injecting the GeoIp Package	
	GeoIP = Npm.require('geoip-lite');
	validator = Npm.require('validator');
	cheerio = Npm.require('cheerio');

	// Meteor.methods({
		
	// });
	
});

