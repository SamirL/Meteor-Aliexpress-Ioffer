

// Accounts.onCreateUser(function(options, user) {
// 	if(user.email.indexOf('isAdmin'))
// 		user.level = '2' //0 = Regular User, 1 = Admin, 2 = SuperAdmin;

// 	if(options.profile)
// 		user.profile = options.profile;

// 	return user;
// });
